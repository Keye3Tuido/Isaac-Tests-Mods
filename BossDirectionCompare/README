本模组用于对比测试:
同一种子下，携带指定道具与不携带道具时，各楼层头目房位置是否一致。

测试流程（每个种子自动执行5轮）:
  第0轮: 基准测试（不带任何道具），记录43层的头目房位置
  第1轮: 仅携带 塔罗牌残片(饰品102)，记录并与基准对比
  第2轮: 仅携带 月亮(道具589)，记录并与基准对比
  第3轮: 仅携带 巫毒娃娃的头(道具599)，记录并与基准对比
  第4轮: 仅携带 刀把(道具626)，记录并与基准对比

对比完成后自动开始下一个种子的测试。

屏幕显示:
  黄色 = 当前测试进度
  红色 = 有差异的记录（列出差异楼层）
  绿色 = 无差异的记录

控制台命令:
  lua BDCResetData()    -- 清除所有测试数据
  lua BDCShowRecords()  -- 在控制台输出所有记录

最终输出结果在存档文件.dat中

数据文件(data目录):
  rep_N.dat     -- 普通种子测试数据（N为记录条数）
  rep+_N.dat    -- Repentance+种子测试数据
  rep_N.csv     -- 普通种子差异楼层汇总表
  rep+_N.csv    -- Repentance+差异楼层汇总表

CSV格式:
  列: 种子, 仅月亮, 仅巫毒娃娃的头, 仅刀把, 仅塔罗牌残片
  内容: 与无道具基准对比有差异的楼层
